# BiteSmart 前端功能设计与联调方案

## 目标

本项目不是普通外卖系统，而是“健康膳食管理 + 在线订餐 + 多角色协同”的业务平台。后续页面设计和功能开发要围绕完整业务闭环推进：

- 用户能完成健康档案、AI 推荐、购买、支付、配送追踪、健康记录、会员与评价。
- 商家能完成入驻、店铺维护、菜品/套餐/库存、订单处理、配送协同、评价回复与经营统计。
- 管理员能完成用户、商家、配送员、营养标准、AI 规则、订单、评论、公告、系统配置和统计管理。
- 小程序端覆盖用户、商家、配送员移动场景，功能可比 PC 端轻，但流程不能断。

## 设计基调

### 不要做成“AI 感”

这里的“AI 感”主要指：大段概念文案、过度渐变、过多装饰卡片、每个页面都像营销首页、数据没有业务优先级。

本项目应采用更像真实餐饮运营后台的设计：

- 页面优先服务“今天要处理什么”，不是展示“这个系统多厉害”。
- 用状态、筛选、待办、异常、批量操作承载业务效率。
- 保持 Element Plus 的组件语义，少造新组件，避免视觉和交互割裂。
- 颜色以深绿为品牌基调，但不要每个模块都只用绿色。异常用红，待处理用橙，完成用绿，信息用灰蓝。
- 卡片只用于独立信息块、表单、列表容器和弹窗，不做装饰性套娃。
- 文案短、直接、可执行，比如“待接单”“库存不足”“未回复评价”，少写“智能赋能经营”这类虚话。

### 参考原则

参考成熟中后台的共同点，不照抄视觉：

- Ant Design 强调企业级产品的稳定组件、统一规范和减少重复建设，适合借鉴页面结构和状态表达。
- Ant Design Pro / Arco Design Pro 的价值在于工作台、列表页、表单页、详情页这些后台范式。
- Element Plus 是当前项目实际组件库，表格、表单、分页、标签、弹窗、空状态要优先使用它的原生能力。

## PC 端功能覆盖设计

### 用户端

当前路由已有：`/user`、`/user/dishes`、`/user/cart`、`/user/orders`、`/user/delivery`、`/user/ai/chat`、`/user/ai/recommend`、`/user/health`、`/user/profile`、`/user/membership`、`/user/addresses`、`/user/reviews`、`/user/notices`。

需要重点核对和补齐：

| 功能 | 页面建议 | 关键联调点 |
| --- | --- | --- |
| 账号密码注册登录 | 登录页 | 登录返回 token、roleType、user 字段必须统一 |
| 问卷式健康档案 | 健康档案页或首次进入引导页 | 档案字段需和 AI 推荐、营养标准共用 |
| AI 智能食谱生成 | AI 推荐页 | 推荐结果要能跳转菜品/套餐购买 |
| AI 实时问答 | AI 对话页 | 对话历史、流式/非流式响应、错误状态 |
| 健康套餐浏览购买 | 套餐/菜品浏览页 | 套餐详情、营养、价格、库存 |
| 套餐内菜品更换 | 套餐详情页 | 替换后价格和营养重新计算 |
| 在线下单 | 购物车/下单页 | 地址、备注、配送方式、订单明细 |
| 在线支付 | 支付页/订单确认页 | 支付状态回写订单 |
| 订单管理 | 我的订单页 | 状态、详情、历史、取消 |
| 未接单前取消 | 订单详情页 | 只有待接单状态可取消 |
| 配送实时定位 | 配送追踪页 | 位置、路线、预计时间 |
| 饮食记录 | 健康记录页 | 热量/蛋白/脂肪/碳水 |
| 运动记录 | 健康记录页 | 类型、时长、距离、消耗 |
| 体重管理 | 健康记录页 | 每日记录和折线趋势 |
| 会员中心 | 会员页 | 套餐购买、权益、到期时间 |

用户端设计重点：不要做成商城首页，要突出“今天吃什么更健康”“这个套餐适合我吗”“吃完如何记录”。

### 管理员端

当前路由基本覆盖管理员 12 项：用户、商家、配送员、分类、营养、AI 规则、订单、评论、公告、统计、系统设置、日志。

需要重点增强：

- 用户管理要能看到健康档案、会员、冻结状态。
- 商家管理要覆盖入驻审核、资质、营业状态。
- 配送员管理要覆盖接单状态和配送范围。
- 数据统计不要只做总数，要按用户、订单、销售额、会员、AI 次数分块。
- 系统管理如果毕业设计时间有限，可以把角色权限、菜单、日志、参数做成可演示闭环，备份作为模拟功能。

管理员端设计重点：偏平台监管，不要做得像商家经营后台。要有审核、风险、异常、全局统计的语义。

### 商家端

当前路由已覆盖商家端 10 项：工作台、店铺、菜品、套餐、订单、库存、配送、评价、分类、销售统计、个人中心。

当前最需要优化的页面优先级：

1. `订单处理`
   - 增加顶部状态筛选：全部、待接单、备餐中、配送中、已完成、已取消。
   - 增加关键计数：待接单、备餐中、超时风险、今日完成。
   - 操作区突出主操作：待接单主按钮是“接单”，拒绝作为弱按钮。
   - 详情弹窗展示菜品明细、用户备注、地址、电话、时间轴。

2. `配送管理`
   - 从单表格改为“配送概览 + 状态分组 + 任务列表”。
   - 增加待分配、待取餐、配送中、异常统计。
   - 支持查看取餐码、配送员电话、预计送达、异常状态。
   - 后续可扩展地图追踪，但当前先保证状态流转可读。

3. `销售统计`
   - 不要和工作台重复。
   - 做成分析页：时间筛选、营收趋势、订单趋势、热销菜品、套餐占比、分类表现。
   - 表格保留，但不作为第一视觉。

4. `库存管理`
   - 当前 tabs 结构可保留。
   - 增加预警概览：库存不足、即将不足、涉及套餐数、今日消耗。
   - 预警列表增加“影响菜品/套餐”视角。

5. `评价管理`
   - 增加筛选：未回复、低分、近 7 天。
   - 评价内容不要只 tooltip，低分评价要能直接看全。
   - 回复弹窗展示原评价、评分、用户、时间。

商家端设计重点：真实商家最关心“订单别漏、出餐别慢、库存别断、差评快回、什么卖得好”。

## 小程序端功能设计

当前小程序目录还偏初始模板，PC 端和后端已经更成熟。因此小程序建议按“移动闭环优先”推进，不要一开始照搬 PC 端所有后台页面。

### 用户小程序

优先实现：

1. 微信登录
2. 健康档案
3. 首页推荐
4. 套餐/菜品浏览
5. 购物车
6. 下单支付
7. 我的订单
8. 配送追踪
9. 健康记录
10. 会员

AI 问答、评论反馈、体重趋势可以第二批补齐，但入口先预留。

### 商家小程序

移动商家端不要做复杂表单，重点是“处理”：

- 今日订单
- 接单/备餐/出餐
- 菜品上下架和库存快捷调整
- 评价回复
- 今日营收和订单数

### 配送员小程序

这是小程序端最需要独立设计的角色：

- 待接单列表
- 配送任务详情
- 状态更新按钮
- 位置上传
- 地图导航
- 异常上报
- 收入统计
- 服务评价

配送员端要围绕“一单一屏”设计，按钮大、状态清楚、地址电话可直接操作。

## 前后端字段契约

为了避免数据库、后端、前端字段对不上，后续每个功能都要按这个顺序做：

1. 确认数据库表字段
2. 确认后端 Entity、DTO、VO 字段
3. 确认 Mapper XML 字段映射
4. 确认 Controller 返回结构
5. 确认前端 api 类型
6. 页面只消费 api 类型，不直接猜后端字段

### 命名规则

- 数据库使用下划线：`order_status`、`create_time`
- Java 使用驼峰：`orderStatus`、`createTime`
- 前端 TypeScript 使用驼峰：`orderStatus`、`createTime`
- 金额字段统一为 number，前端展示时格式化为 `¥xx`
- 状态字段统一用 number code，前端提供集中映射函数
- 时间字段统一后端格式，前端只做展示格式化

### 每个接口必须明确

```ts
interface ApiResponse<T> {
  code: number
  message: string
  data: T
}
```

分页接口必须统一：

```ts
interface PageData<T> {
  list: T[]
  total: number
  pageNum?: number
  pageSize?: number
}
```

当前项目里部分接口使用 `res.data.list`，部分使用 `res.data.data.list`，这是后续最容易出错的点，需要统一或在 api 层做兼容，不要散落在页面里。

## 页面开发标准

### 列表页

列表页至少包含：

- 页面标题和简短副标题
- 顶部筛选区
- 状态统计或关键待办
- 表格
- 分页
- 空状态
- 详情/编辑/删除等操作反馈

### 表单页

表单页至少包含：

- 基础信息分组
- 必填校验
- 保存前数据格式转换
- 取消/保存按钮位置统一
- 上传图片预览和删除
- 保存成功后刷新或同步全局状态

### 工作台

工作台只展示今日最重要的东西：

- 今日营收
- 今日订单
- 待处理订单
- 库存预警
- 待回复评价
- 热销菜品
- 近 7 日趋势

不要把所有功能入口都塞进工作台。

## 开发顺序建议

### 第一阶段：商家端闭环

1. 订单处理页
2. 配送管理页
3. 库存管理页
4. 销售统计页
5. 评价管理页

先把商家端跑通，因为它连接菜品、套餐、订单、配送、评价，是平台业务中间层。

### 第二阶段：用户端闭环

1. 健康档案
2. 套餐详情和替换
3. 下单支付
4. 订单详情和取消
5. 配送追踪
6. 健康记录和会员

用户端要保证“推荐 -> 购买 -> 配送 -> 记录 -> 复购”闭环。

### 第三阶段：管理员端闭环

1. 商家审核
2. 用户和配送员管理
3. 营养标准和 AI 规则
4. 订单异常和评论投诉
5. 平台统计
6. 系统管理

管理员端是监管和配置，不要先花太多时间做视觉花样。

### 第四阶段：小程序端

小程序先做用户端下单闭环和配送员端接单闭环，再补商家移动端。

## 下一步执行建议

从当前项目状态看，下一步不建议继续大改工作台。工作台已经有方向，但需要稍微收敛得更像中后台。

最值得立刻做的是：

1. 重做商家端 `订单处理`
2. 重做商家端 `配送管理`
3. 把商家端列表页的页面头、筛选区、空状态、分页样式统一
4. 建一个 api 类型契约检查清单，逐个页面核对字段

这样做完以后，商家端会从“页面齐了”变成“业务真的能跑”。
