<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.order.OrderItemMapper">

    <sql id="BaseColumns">
        id, order_id, item_type, dish_id, combo_id,
        snapshot_name, snapshot_image, snapshot_price,
        snapshot_calories, snapshot_protein, snapshot_fat, snapshot_carbs,
        snapshot_nutrition_json,
        quantity, sub_total, create_time, update_time, deleted
    </sql>

    <!-- 查订单的所有明细 -->
    <select id="findByOrderId" resultType="com.ws.bitesmart.entity.order.OrderItem">
        SELECT <include refid="BaseColumns"/>
        FROM order_item
        WHERE order_id = #{orderId} AND deleted = 0
        ORDER BY id ASC
    </select>

    <!-- 批量插入订单明细 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_item (id, order_id, item_type, dish_id, combo_id,
                                snapshot_name, snapshot_image, snapshot_price,
                                snapshot_calories, snapshot_protein, snapshot_fat, snapshot_carbs,
                                snapshot_nutrition_json, quantity, sub_total,
                                create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.orderId}, #{item.itemType}, #{item.dishId}, #{item.comboId},
             #{item.snapshotName}, #{item.snapshotImage}, #{item.snapshotPrice},
             #{item.snapshotCalories}, #{item.snapshotProtein}, #{item.snapshotFat}, #{item.snapshotCarbs},
             #{item.snapshotNutritionJson}, #{item.quantity}, #{item.subTotal},
             NOW(), NOW())
        </foreach>
    </insert>

</mapper>
