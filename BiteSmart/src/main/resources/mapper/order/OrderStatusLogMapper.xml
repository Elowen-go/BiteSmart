<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.order.OrderStatusLogMapper">
    <select id="findByOrderId" resultType="com.ws.bitesmart.entity.order.OrderStatusLog">
        SELECT id, order_id, order_no, from_status, to_status, operator_id,
               operator_type, reason, source, create_time
        FROM order_status_log
        WHERE order_id = #{orderId}
        ORDER BY create_time ASC, id ASC
    </select>
</mapper>
