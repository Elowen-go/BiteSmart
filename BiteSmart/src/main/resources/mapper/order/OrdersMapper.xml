<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.order.OrdersMapper">

    <sql id="BaseColumns">
        id, order_no, user_id, merchant_id, delivery_driver_id,
        total_amount, discount_amount, pay_amount, delivery_type, delivery_fee, pay_method, pay_time,
        order_status, delivery_status, delivery_address, delivery_lat, delivery_lng,
        receiver_name, receiver_phone, remark,
        cancel_time, cancel_reason, finish_time,
        refund_id, coupon_id, coupon_discount, platform_subsidy,
        lock_stock_time, auto_cancel_time, channel,
        create_time, update_time, deleted
    </sql>

    <!-- 按订单号查 -->
    <select id="findByOrderNo" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE order_no = #{orderNo} AND deleted = 0
    </select>

    <!-- 按ID查 -->
    <select id="findById" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 用户查自己的订单 -->
    <select id="findByUserId" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 商家查收到的订单 -->
      <select id="findByMerchantId" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE merchant_id = #{merchantId} AND deleted = 0
        ORDER BY create_time DESC
      </select>

      <select id="findPaidByMerchantId" resultType="com.ws.bitesmart.entity.order.Orders">
          SELECT <include refid="BaseColumns"/>
          FROM orders
          WHERE merchant_id = #{merchantId}
            AND order_status IN (20, 30, 40, 50)
            AND pay_time IS NOT NULL
            AND deleted = 0
          ORDER BY pay_time ASC
      </select>

    <!-- 新增订单 -->
    <insert id="insert" parameterType="com.ws.bitesmart.entity.order.Orders">
        INSERT INTO orders (id, order_no, user_id, merchant_id, delivery_driver_id,
                            total_amount, discount_amount, pay_amount, delivery_type, delivery_fee, pay_method, pay_time,
                            order_status, delivery_status, delivery_address, delivery_lat, delivery_lng,
                            receiver_name, receiver_phone, remark,
                            cancel_time, cancel_reason, finish_time,
                            refund_id, coupon_id, coupon_discount, platform_subsidy,
                            lock_stock_time, auto_cancel_time, channel,
                            create_time, update_time)
        VALUES (#{id}, #{orderNo}, #{userId}, #{merchantId}, #{deliveryDriverId},
                #{totalAmount}, #{discountAmount}, #{payAmount}, #{deliveryType}, #{deliveryFee}, #{payMethod}, #{payTime},
                #{orderStatus}, #{deliveryStatus}, #{deliveryAddress}, #{deliveryLat}, #{deliveryLng},
                #{receiverName}, #{receiverPhone}, #{remark},
                #{cancelTime}, #{cancelReason}, #{finishTime},
                #{refundId}, #{couponId}, #{couponDiscount}, #{platformSubsidy},
                #{lockStockTime}, #{autoCancelTime}, #{channel},
                NOW(), NOW())
    </insert>

    <!-- 更新订单状态 -->
    <update id="updateStatus">
        UPDATE orders
        <set>
            order_status = #{orderStatus},
            <if test="payMethod != null">pay_method = #{payMethod},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="cancelTime != null">cancel_time = #{cancelTime},</if>
            <if test="cancelReason != null">cancel_reason = #{cancelReason},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
            <if test="payAmount != null">pay_amount = #{payAmount},</if>
            <if test="deliveryStatus != null">delivery_status = #{deliveryStatus},</if>
            <if test="refundId != null">refund_id = #{refundId},</if>
            <if test="couponId != null">coupon_id = #{couponId},</if>
            <if test="couponDiscount != null">coupon_discount = #{couponDiscount},</if>
            <if test="platformSubsidy != null">platform_subsidy = #{platformSubsidy},</if>
            <if test="lockStockTime != null">lock_stock_time = #{lockStockTime},</if>
            <if test="autoCancelTime != null">auto_cancel_time = #{autoCancelTime},</if>
            <if test="channel != null">channel = #{channel},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 带乐观锁的订单状态更新：仅当当前状态符合预期时才更新 -->
    <update id="updateStatusWithLock">
        UPDATE orders
        SET order_status = #{orderStatus},
            <if test="payMethod != null">pay_method = #{payMethod},</if>
            <if test="payAmount != null">pay_amount = #{payAmount},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="cancelTime != null">cancel_time = #{cancelTime},</if>
            <if test="cancelReason != null">cancel_reason = #{cancelReason},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
            <if test="deliveryStatus != null">delivery_status = #{deliveryStatus},</if>
            update_time = NOW()
        WHERE id = #{id}
          AND order_status = #{expectedStatus}
          AND deleted = 0
    </update>

    <!-- 统计某商家在指定时间范围内的已完成订单数（状态=50 已完成） -->
    <select id="countByMerchantAndTime" resultType="int">
        SELECT COUNT(*)
        FROM orders
        WHERE merchant_id = #{merchantId}
          AND order_status &lt;&gt; 60
          AND create_time BETWEEN #{start} AND #{end}
          AND deleted = 0
    </select>

    <!-- 统计某商家在指定时间范围内的已完成订单销售额 -->
    <select id="sumPayAmountByMerchantAndTime" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(pay_amount), 0)
        FROM orders
        WHERE merchant_id = #{merchantId}
          AND order_status IN (20, 30, 40, 50, 70, 80)
          AND create_time BETWEEN #{start} AND #{end}
          AND deleted = 0
    </select>

    <select id="countPendingByMerchantAndTime" resultType="int">
        SELECT COUNT(*)
        FROM orders
        WHERE merchant_id = #{merchantId}
          AND order_status IN (20, 30)
          AND create_time BETWEEN #{start} AND #{end}
          AND deleted = 0
    </select>

    <select id="countDistinctUsersByMerchantAndTime" resultType="int">
        SELECT COUNT(DISTINCT user_id)
        FROM orders
        WHERE merchant_id = #{merchantId}
          AND order_status &lt;&gt; 60
          AND create_time BETWEEN #{start} AND #{end}
          AND deleted = 0
    </select>

    <!-- 分页查询所有订单 -->
    <select id="findAll" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="findAllByStatus" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE deleted = 0
          AND order_status = #{orderStatus}
        ORDER BY create_time DESC
    </select>

    <select id="findAdminList" resultType="com.ws.bitesmart.entity.order.Orders">
        SELECT <include refid="BaseColumns"/>
        FROM orders
        WHERE deleted = 0
        <if test="userId != null">AND user_id = #{userId}</if>
        <if test="merchantId != null">AND merchant_id = #{merchantId}</if>
        <if test="paymentStatus == 10">AND (pay_time IS NOT NULL OR order_status IN (20, 30, 40, 50, 70, 80))</if>
        <if test="paymentStatus == 0">AND pay_time IS NULL AND order_status = 10</if>
        <if test="deliveryStatus != null">AND delivery_status = #{deliveryStatus}</if>
        <if test="orderStatus != null">AND order_status = #{orderStatus}</if>
        ORDER BY create_time DESC
    </select>

    <!-- 统计所有订单数量 -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM orders WHERE deleted = 0
    </select>

    <!-- 统计所有订单实付金额总和 -->
    <select id="sumPayAmountAll" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(pay_amount), 0) FROM orders WHERE deleted = 0
    </select>

    <!-- 统计指定时间范围内的订单数 -->
    <select id="countByTimeRange" resultType="int">
        SELECT COUNT(*) FROM orders
        WHERE deleted = 0
          AND create_time BETWEEN #{start} AND #{end}
    </select>

    <!-- 统计指定时间范围内的实付金额总和 -->
    <select id="sumPayAmountByTimeRange" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(pay_amount), 0) FROM orders
        WHERE deleted = 0
          AND create_time BETWEEN #{start} AND #{end}
    </select>

    <select id="aggregateDaily" resultType="map">
        SELECT DATE_FORMAT(create_time, '%Y-%m-%d') AS stat_date,
               COUNT(*) AS order_count,
               COALESCE(SUM(pay_amount), 0) AS revenue
        FROM orders
        WHERE deleted = 0
          AND create_time BETWEEN #{start} AND #{end}
        GROUP BY DATE_FORMAT(create_time, '%Y-%m-%d')
        ORDER BY stat_date ASC
    </select>

    <select id="aggregateByStatus" resultType="map">
        SELECT order_status, COUNT(*) AS order_count
        FROM orders
        WHERE deleted = 0
        GROUP BY order_status
        ORDER BY order_status ASC
    </select>

</mapper>
