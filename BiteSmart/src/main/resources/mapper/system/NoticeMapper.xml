<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.system.NoticeMapper">

    <sql id="BaseColumns">
        id, title, content, notice_type, target_role, priority, status, publish_time, expire_time,
        create_time, update_time, deleted
    </sql>

    <select id="findAll" resultType="com.ws.bitesmart.entity.system.Notice">
        SELECT <include refid="BaseColumns"/>
        FROM system_notice
        WHERE deleted = 0
        ORDER BY publish_time DESC
    </select>

    <select id="findPublished" resultType="com.ws.bitesmart.entity.system.Notice">
        SELECT <include refid="BaseColumns"/>
        FROM system_notice
        WHERE status = 20 AND deleted = 0
        ORDER BY publish_time DESC
    </select>

    <select id="findById" resultType="com.ws.bitesmart.entity.system.Notice">
        SELECT <include refid="BaseColumns"/>
        FROM system_notice
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.ws.bitesmart.entity.system.Notice">
        INSERT INTO system_notice (id, title, content, notice_type, target_role, priority, status, publish_time, expire_time, create_time, update_time)
        VALUES (#{id}, #{title}, #{content}, #{noticeType}, #{targetRole}, #{priority}, #{status}, #{publishTime}, #{expireTime}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.ws.bitesmart.entity.system.Notice">
        UPDATE system_notice
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="noticeType != null">notice_type = #{noticeType},</if>
            <if test="targetRole != null">target_role = #{targetRole},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="status != null">status = #{status},</if>
            <if test="publishTime != null">publish_time = #{publishTime},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE system_notice
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>