<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.UserAddressMapper">

    <sql id="BaseColumns">
        id, user_id, receiver_name, receiver_phone, province, city, district,
        detail_address, latitude, longitude, address_tag, is_default,
        create_time, update_time, deleted
    </sql>

    <select id="findByUserId" resultType="com.ws.bitesmart.entity.UserAddress">
        SELECT <include refid="BaseColumns"/>
        FROM user_address
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="findById" resultType="com.ws.bitesmart.entity.UserAddress">
        SELECT <include refid="BaseColumns"/>
        FROM user_address
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.ws.bitesmart.entity.UserAddress">
        INSERT INTO user_address (id, user_id, receiver_name, receiver_phone, province, city, district,
                                  detail_address, latitude, longitude, address_tag, is_default,
                                  create_time, update_time)
        VALUES (#{id}, #{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district},
                #{detailAddress}, #{latitude}, #{longitude}, #{addressTag}, #{isDefault},
                NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.ws.bitesmart.entity.UserAddress">
        UPDATE user_address
        <set>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="latitude != null">latitude = #{latitude},</if>
            <if test="longitude != null">longitude = #{longitude},</if>
            <if test="addressTag != null">address_tag = #{addressTag},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="resetDefault">
        UPDATE user_address SET is_default = 0
        WHERE user_id = #{userId} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE user_address SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
