<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.dish.InventoryLogMapper">

    <sql id="BaseColumns">
        id, dish_id, merchant_id, change_type, change_quantity,
        before_stock, after_stock, before_lock_stock, after_lock_stock,
        biz_type, biz_id, operator_id, remark, create_time, deleted
    </sql>

    <!-- 查某商家的库存变动记录，按时间倒序 -->
    <select id="findByMerchantId" resultType="com.ws.bitesmart.entity.dish.InventoryLog">
        SELECT <include refid="BaseColumns"/>
        FROM inventory_log
        WHERE merchant_id = #{merchantId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
