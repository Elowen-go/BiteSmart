<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.user.MembershipPlanMapper">

    <sql id="BaseColumns">
        id, plan_name, plan_type, price, original_price, valid_days,
        benefits, status, sort_order, create_time, update_time, deleted
    </sql>

    <!-- 用户端：只展示已上架的套餐，按 sort_order 排序 -->
    <select id="findAvailable" resultType="com.ws.bitesmart.entity.user.MembershipPlan">
        SELECT <include refid="BaseColumns"/>
        FROM membership_plan
        WHERE status = 10 AND deleted = 0
        ORDER BY sort_order ASC
    </select>

    <select id="findById" resultType="com.ws.bitesmart.entity.user.MembershipPlan">
        SELECT <include refid="BaseColumns"/>
        FROM membership_plan
        WHERE id = #{id} AND deleted = 0
    </select>

</mapper>
