<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.ai.AiRecommendRuleMapper">

    <sql id="BaseColumns">
        id, rule_name, target_goal, prompt_template, calorie_float,
        max_calories, min_calories, protein_ratio, fat_ratio, carbs_ratio,
        model_version, status, priority, create_time, update_time, deleted
    </sql>

    <!-- 查匹配目标且启用的规则，按优先级取最高的一条 -->
    <select id="findBestRuleByGoal" resultType="com.ws.bitesmart.entity.ai.AiRecommendRule">
        SELECT <include refid="BaseColumns"/>
        FROM ai_recommend_rule
        WHERE target_goal = #{targetGoal}
          AND status = 10
          AND deleted = 0
        ORDER BY priority DESC
        LIMIT 1
    </select>

    <!-- 查询所有规则，按优先级排序 -->
    <select id="findAll" resultType="com.ws.bitesmart.entity.ai.AiRecommendRule">
        SELECT <include refid="BaseColumns"/>
        FROM ai_recommend_rule
        WHERE deleted = 0
        ORDER BY priority DESC
    </select>

    <!-- 新增规则 -->
    <insert id="insert" parameterType="com.ws.bitesmart.entity.ai.AiRecommendRule">
        INSERT INTO ai_recommend_rule (id, rule_name, target_goal, prompt_template, calorie_float,
                                       max_calories, min_calories, protein_ratio, fat_ratio, carbs_ratio,
                                       model_version, status, priority, create_time, update_time)
        VALUES (#{id}, #{ruleName}, #{targetGoal}, #{promptTemplate}, #{calorieFloat},
                #{maxCalories}, #{minCalories}, #{proteinRatio}, #{fatRatio}, #{carbsRatio},
                #{modelVersion}, #{status}, #{priority}, NOW(), NOW())
    </insert>

    <!-- 更新规则 -->
    <update id="updateById" parameterType="com.ws.bitesmart.entity.ai.AiRecommendRule">
        UPDATE ai_recommend_rule
        <set>
            <if test="ruleName != null">rule_name = #{ruleName},</if>
            <if test="targetGoal != null">target_goal = #{targetGoal},</if>
            <if test="promptTemplate != null">prompt_template = #{promptTemplate},</if>
            <if test="calorieFloat != null">calorie_float = #{calorieFloat},</if>
            <if test="maxCalories != null">max_calories = #{maxCalories},</if>
            <if test="minCalories != null">min_calories = #{minCalories},</if>
            <if test="proteinRatio != null">protein_ratio = #{proteinRatio},</if>
            <if test="fatRatio != null">fat_ratio = #{fatRatio},</if>
            <if test="carbsRatio != null">carbs_ratio = #{carbsRatio},</if>
            <if test="modelVersion != null">model_version = #{modelVersion},</if>
            <if test="status != null">status = #{status},</if>
            <if test="priority != null">priority = #{priority},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE ai_recommend_rule
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
