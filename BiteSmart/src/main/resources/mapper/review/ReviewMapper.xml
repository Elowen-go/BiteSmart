<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.review.ReviewMapper">

    <sql id="BaseColumns">
        id, order_id, user_id, merchant_id, driver_id, dish_id,
        rating_food, rating_delivery, rating_service, overall_rating,
        content, images, is_anonymous,
        merchant_reply, merchant_reply_time,
        status, create_time, update_time, deleted
    </sql>

    <!-- 根据 ID 查询评价 -->
    <select id="findById" resultType="com.ws.bitesmart.entity.review.Review">
        SELECT <include refid="BaseColumns"/>
        FROM review WHERE id = #{id} AND deleted = 0
    </select>

    <!-- 根据订单 ID 查询评价 -->
    <select id="findByOrderId" resultType="com.ws.bitesmart.entity.review.Review">
        SELECT <include refid="BaseColumns"/>
        FROM review WHERE order_id = #{orderId} AND deleted = 0
    </select>

    <!-- 查某商家的评价列表，按时间倒序 -->
    <select id="findByMerchantId" resultType="com.ws.bitesmart.entity.review.Review">
        SELECT <include refid="BaseColumns"/>
        FROM review
        WHERE merchant_id = #{merchantId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 查某用户的评价列表，按时间倒序 -->
    <select id="findByUserId" resultType="com.ws.bitesmart.entity.review.Review">
        SELECT <include refid="BaseColumns"/>
        FROM review
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 查某配送员的评价列表，按时间倒序 -->
    <select id="findByDriverId" resultType="com.ws.bitesmart.entity.review.Review">
        SELECT <include refid="BaseColumns"/>
        FROM review
        WHERE driver_id = #{driverId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 新增评价 -->
    <insert id="insert" parameterType="com.ws.bitesmart.entity.review.Review">
        INSERT INTO review (id, order_id, user_id, merchant_id, driver_id, dish_id,
                            rating_food, rating_delivery, rating_service, overall_rating,
                            content, images, is_anonymous, status,
                            create_time, update_time)
        VALUES (#{id}, #{orderId}, #{userId}, #{merchantId}, #{driverId}, #{dishId},
                #{ratingFood}, #{ratingDelivery}, #{ratingService}, #{overallRating},
                #{content}, #{images}, #{isAnonymous}, #{status},
                NOW(), NOW())
    </insert>

    <!-- 商家回复评价 -->
    <update id="updateReply">
        UPDATE review
        SET merchant_reply = #{merchantReply},
            merchant_reply_time = #{merchantReplyTime},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新评价状态（管理员隐藏/删除） -->
    <update id="updateStatus">
        UPDATE review
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
