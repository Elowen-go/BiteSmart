<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.SysUserMapper">

    <!-- 通用查询列，所有查询都复用这一段 -->
    <sql id="BaseColumns">
        id, username, password, nickname, avatar, phone, email,
        role_type, status, register_source,
        last_login_time, last_login_ip, create_time, update_time, deleted
    </sql>

    <!-- 根据用户名查用户 -->
    <select id="findByUsername" resultType="com.ws.bitesmart.entity.SysUser">
        SELECT <include refid="BaseColumns"/>
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
    </select>

    <!-- 根据ID查用户 -->
    <select id="findById" resultType="com.ws.bitesmart.entity.SysUser">
        SELECT <include refid="BaseColumns"/>
        FROM sys_user
        WHERE id = #{id}
          AND deleted = 0
    </select>

    <!-- 新增用户 -->
    <insert id="insert" parameterType="com.ws.bitesmart.entity.SysUser">
        INSERT INTO sys_user (id, username, password, nickname, avatar, phone, email,
                              role_type, status, register_source, create_time, update_time)
        VALUES (#{id}, #{username}, #{password}, #{nickname}, #{avatar}, #{phone}, #{email},
                #{roleType}, #{status}, #{registerSource}, NOW(), NOW())
    </insert>

    <!-- 根据ID更新用户（只更新非空字段） -->
    <update id="updateById" parameterType="com.ws.bitesmart.entity.SysUser">
        UPDATE sys_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 更新最后登录时间和IP -->
    <update id="updateLoginTime">
        UPDATE sys_user
        SET last_login_time = #{lastLoginTime},
            last_login_ip = #{lastLoginIp}
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
