<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.health.ExerciseLibraryMapper">

    <select id="findAll" resultType="com.ws.bitesmart.entity.health.ExerciseLibrary">
        SELECT id, category, name, std_text, kcal_per_min, def_mins, def_dist,
               image_url, tags, hot, sort, create_time, update_time, deleted
        FROM exercise_library
        WHERE deleted = 0
        ORDER BY sort ASC, id ASC
    </select>

</mapper>
