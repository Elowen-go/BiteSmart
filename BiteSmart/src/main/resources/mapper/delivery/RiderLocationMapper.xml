<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ws.bitesmart.mapper.delivery.RiderLocationMapper">

    <insert id="insert" parameterType="com.ws.bitesmart.entity.delivery.RiderLocation">
        INSERT INTO rider_location (id, task_id, driver_id, latitude, longitude, created_time)
        VALUES (#{id}, #{taskId}, #{driverId}, #{latitude}, #{longitude}, NOW())
    </insert>

    <select id="findByTaskId" resultType="com.ws.bitesmart.entity.delivery.RiderLocation">
        SELECT id, task_id, driver_id, latitude, longitude, created_time
        FROM rider_location
        WHERE task_id = #{taskId}
        ORDER BY created_time ASC, id ASC
    </select>

</mapper>
