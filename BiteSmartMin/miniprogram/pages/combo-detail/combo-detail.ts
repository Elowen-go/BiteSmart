import { addToCart } from '../../api/cart'
import { getCombo } from '../../api/catalog'
import { requireUser } from '../../utils/user-route'
Page({ data: { combo: { id: 1, name: '7 天轻松减脂计划', desc: '高蛋白 · 控热量 · 每日三餐', price: '98', kcal: '1500 kcal/天', image: 'https://images.unsplash.com/photo-1498837167922-ddd27525d352?w=900&q=80' } }, onLoad(options: Record<string, string>) { if (!requireUser()) return; const id = Number(options?.id || 1); getCombo(id).then((result) => { const combo = (result.combo || result) as Record<string, unknown>; this.setData({ combo: { ...this.data.combo, id, name: String(combo.name || combo.comboName || this.data.combo.name), desc: String(combo.description || this.data.combo.desc), price: String(combo.price || this.data.combo.price), kcal: `${combo.calories || 1500} kcal/天`, image: String(combo.image || this.data.combo.image) } }) }).catch(() => {}) }, back() { wx.navigateBack() }, add() { addToCart(20, this.data.combo.id).then(() => wx.showToast({ title: '套餐已加入购物车', icon: 'none' })).catch((error: Error) => wx.showToast({ title: error.message || '加入失败', icon: 'none' })) } })
