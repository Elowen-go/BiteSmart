<view class="user-page"><view class="back-row"><text class="back" bindtap="back">‹</text><text>健康套餐</text><text class="accent">购物车</text></view><text class="eyebrow">SMART MEAL PLANS</text><text class="title">为目标准备的，<br/>一整套好好吃饭。</text><view class="combo-list"><block wx:for="{{items}}" wx:key="id"><view class="combo-card" data-id="{{item.id}}" bindtap="openDetail"><image src="{{item.image}}" mode="aspectFill"/><view class="combo-body"><text class="combo-name">{{item.name}}</text><text class="combo-desc">{{item.desc}}</text><view><text class="combo-price">¥{{item.price}}</text><text class="combo-meta">{{item.days}} 天 · {{item.kcal}}</text></view></view></view></block></view></view>
